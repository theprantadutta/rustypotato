version: '3.8'

services:
  rustypotato:
    image: rustypotato/rustypotato:latest
    container_name: rustypotato
    ports:
      # Main RESP port. Match the `port` value in
      # docker/rustypotato.toml (default: 6379).
      - "6379:6379"
      # Monitoring/health/metrics endpoint. The bin currently binds
      # this at `port + 1000` (so 7379 for the default port). If
      # you change the main port, change this too.
      - "7379:7379"
    volumes:
      - rustypotato-data:/data
      - ./docker/rustypotato.toml:/etc/rustypotato/rustypotato.toml:ro
    command: >
      rustypotato-server
      --config /etc/rustypotato/rustypotato.toml
      --bind 0.0.0.0
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rustypotato-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: rustypotato-prometheus
    ports:
      - "9091:9090"
    volumes:
      - ./docker/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    depends_on:
      - rustypotato
    restart: unless-stopped

  # Optional: Grafana for visualization
  grafana:
    image: grafana/grafana:latest
    container_name: rustypotato-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus
    restart: unless-stopped

volumes:
  rustypotato-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

networks:
  default:
    name: rustypotato-network